
from setuptools import setup
from Swarm import version

setup(
	name = 'Swarm',
	version = version,
	description = 'TODO',
	author = "None",
	author_email = "None",
	scripts = ['Swarm.py'],
	packages = ['Swarm'],
	install_requires = ["setuptools", ])
